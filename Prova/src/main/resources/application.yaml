spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/dbproduto
    username: root
    password:  
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.mariadb.hibernate.dialect.MariaDBDialect
